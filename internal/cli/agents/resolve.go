// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package agents

import (
	"fmt"
	"github.com/dalmia/calibrate-cli/internal/client"
	"github.com/dalmia/calibrate-cli/internal/flagutil"
	"github.com/dalmia/calibrate-cli/internal/interactive"
	"github.com/dalmia/calibrate-cli/internal/output"
	"github.com/dalmia/calibrate-cli/internal/sdk"
	"github.com/dalmia/calibrate-cli/internal/sdk/models/operations"
	"github.com/dalmia/calibrate-cli/internal/usage"
	"github.com/spf13/cobra"
)

var resolveCmdMeta = []flagutil.FlagMeta{
	{FlagName: "x-api-key", Shorthand: "x", FieldPath: "XAPIKey", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `header:"style=simple,explode=false,name=X-API-Key"`, Description: "string value"},
	{FlagName: "names", Shorthand: "n", FieldPath: "Body.Names", Kind: flagutil.FlagKindStringArray, Required: true, Description: "Agent names to resolve to IDs [required]"},
}

// initResolveCmd initializes the resolve command.
func initResolveCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "resolve",
		Short:   "Resolve agent names to IDs",
		Long:    "Get the IDs for your agents by their names",
		Example: "  calibrate agents resolve --names '[\"my-agent\",\"support-bot\"]'",
		RunE:    runResolveCmd,
	}
	flagutil.RegisterFlags(cmd, resolveCmdMeta)
	if err := flagutil.ValidateMeta[operations.ResolveAgentNamesAgentsResolvePostRequest](resolveCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for resolve: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runResolveCmd executes the resolve command.
func runResolveCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, resolveCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, resolveCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.ResolveAgentNamesAgentsResolvePostRequest](cmd, resolveCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Agents.Resolve(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
